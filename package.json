{
  "name": "link-hub-pro",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "http-server public -p 5173 -c-1 --cors",
    "start": "http-server public -p $PORT",
    "build": "echo 'Static site - nothing to build'"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
